%h1 Listing maps
#map-canvas{:style => "width: 400px; height: 300px"}
%table
  %tr
    %th Title
    %th Body
    %th
    %th
    %th

  - @maps.each do |map|
    %tr
      %td= map.title
      %td= map.body
      %td= link_to 'Show', map
      %td= link_to 'Edit', edit_map_path(map)
      %td= link_to 'Destroy', map, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Map', new_map_path
